# Dependencies
node_modules/

# Build
dist/
src-tauri/target/
# Regenerated from the capability/permission definitions on every build, and
# nothing here consumes them (no $schema refs in src-tauri/capabilities/, no
# mention in CI) — they only ever churned review diffs when a toolchain moved.
# Note this is gen/schemas alone: gen/apple and gen/android are hand-maintained
# (project.yml, Info.plist, entitlements, the Android launcher icons).
src-tauri/gen/schemas/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.local

# Android release signing — never commit real keystores or their passwords
keystore.properties
*.jks
*.keystore

# Flatpak
flatpak-build/
flatpak-repo/
.flatpak-builder/
*.flatpak
repo/

# Nix build output symlinks
result
result-*

# direnv
.direnv/

# Superpowers brainstorm scratch (visual companion mockups)
.superpowers/

# Claude Code worktrees
.claude/worktrees/

# Python bytecode from scripts/
__pycache__/
